/* AUTO-GENERATED by scripts/generate-from-registry.mts — DO NOT EDIT. Regenerate with: npm run generate */
import type { GeneratedResource } from './types';

export const skillsResource: GeneratedResource = {
	resource: 'skills',
	displayName: 'Skills',
	operations: [
		{
			value: 'createFileSkill',
			name: 'Create File Skill',
			action: 'Create a file-skill from scratch — its SKILL.md and, if needed, its scripts and references — in a single call',
			description: 'Create a file-skill from scratch — its SKILL.md and, if needed, its scripts and references — in a single call. A skill is TEXT injected into the prompt of the AI collaborators: write it for the person, at their request, and tell them what it will contain. `files` MUST contain an entry whose path is exactly `SKILL.md` (without it, 400: that file IS the skill). Give each file\'s content as `content`, in PLAIN TEXT — the server does the encoding. DO NOT ENCODE ANYTHING YOURSELF: a malformed base64 produces an unreadable file with NO error at all, and since a skill is published the moment it is created, that unreadable file goes straight into the prompt of every collaborator carrying it. `content_base64` is still there for a file that is NOT text, and exactly one of the two is required per file — sending both is refused rather than arbitrated. `description` is not decorative: it is what triggers the automatic loading of the skill, so write it as a « when to use this ». `is_draft: true` creates a DRAFT, inert until `update_file_skill(is_draft: false)` publishes it — by default (`is_draft` absent) the skill is published immediately and will be injected at the next load of every collaborator carrying it. Limits: 50 MB total, 100 files, 4 folder levels. Read the result back with `list_file_skills` (the only tool that returns the `ID` the other seven skill tools need). To add a file afterwards use `write_file_skill_file`, which takes RAW text; for an image or a PDF, the upload only exists in the app.',
			routeSpec: {"method":"POST","path":"/api/skills","queryParams":[]},
			properties: [
				{
					displayName: 'Name',
					name: 'name',
					type: 'string',
					required: true,
					description: 'Name of the skill',
					default: '',
				},
				{
					displayName: 'Description',
					name: 'description',
					type: 'string',
					required: true,
					description: '« When to use this » — this text is what triggers automatic loading',
					default: '',
				},
				{
					displayName: 'Files',
					name: 'files',
					type: 'json',
					required: true,
					description: 'Must contain an entry whose path is exactly « SKILL.md ». (provide a JSON array).',
					default: '[]',
				},
				{
					displayName: 'Additional Fields',
					name: 'additionalFields',
					type: 'collection',
					placeholder: 'Add Field',
					default: {},
					options: [
						{
							displayName: 'Is Draft',
							name: 'is_draft',
							type: 'boolean',
							description: 'Whether true = inert draft, to be published later with update_file_skill(is_draft:false). Absent = published immediately.',
							default: false,
						},
					],
				}
			],
		},
		{
			value: 'createFileSkillFolder',
			name: 'Create File Skill Folder',
			action: 'Create an EMPTY, persistent folder inside a file skill (a hidden `.keep` marker is dropped so the empty folder survives)',
			description: 'Create an EMPTY, persistent folder inside a file skill (a hidden `.keep` marker is dropped so the empty folder survives). Only worth it to organise before writing several files; for a single file, `write_file_skill_file` creates the folders on its own. 4 levels deep max. Owner only.',
			routeSpec: {"method":"POST","path":"/api/skills/{id}/files/folder","queryParams":[]},
			properties: [
				{
					displayName: 'ID',
					name: 'id',
					type: 'string',
					required: true,
					description: 'The ID for this operation',
					default: '',
				},
				{
					displayName: 'Dir',
					name: 'dir',
					type: 'string',
					required: true,
					description: 'Folder path, e.g. references/',
					default: '',
				}
			],
		},
		{
			value: 'deleteFileSkill',
			name: 'Delete File Skill',
			action: 'Delete a file skill AND all of its files (SKILL.md, scripts, references), for good',
			description: 'Delete a file skill AND all of its files (SKILL.md, scripts, references), for good. No bin, no restore route. To only stop it from loading, `toggle_file_skill` does that and keeps everything. Owner only, on an explicit request naming the skill.',
			routeSpec: {"method":"DELETE","path":"/api/skills/{id}","queryParams":[]},
			properties: [
				{
					displayName: 'ID',
					name: 'id',
					type: 'string',
					required: true,
					description: 'The ID for this operation',
					default: '',
				}
			],
		},
		{
			value: 'deleteFileSkillFile',
			name: 'Delete File Skill File',
			action: 'Delete ONE file of a file skill',
			description: 'Delete ONE file of a file skill. Never `SKILL.md` — the service refuses it, because that file IS the skill (to delete the whole thing, `delete_file_skill`). Irreversible, no bin. Owner only. `path` is the exact relative path as the skill\'s file listing returns it.',
			routeSpec: {"method":"DELETE","path":"/api/skills/{id}/files","queryParams":["path"]},
			properties: [
				{
					displayName: 'ID',
					name: 'id',
					type: 'string',
					required: true,
					description: 'The ID for this operation',
					default: '',
				},
				{
					displayName: 'Path',
					name: 'path',
					type: 'string',
					required: true,
					description: 'Relative path of the file. SKILL.md is refused.',
					default: '',
				}
			],
		},
		{
			value: 'importFileSkillFromGithub',
			name: 'Import File Skill From Github',
			action: 'Import a file skill from a PUBLIC GitHub repository (`githubUrl` = the repo URL, or a subfolder that contains a SKILL.md)',
			description: 'Import a file skill from a PUBLIC GitHub repository (`githubUrl` = the repo URL, or a subfolder that contains a SKILL.md). An imported skill is TEXT that will be injected into the collaborators\' prompts: import it only on an explicit request, from a source the person named, and hand them the injection scan result (`injection_severity`, `injection_signals`) BEFORE they enable it. Archive 50 MB max, zip-bomb guarded. The other import path (a `.skill` or `.zip` file) is a browser upload, not this tool.',
			routeSpec: {"method":"POST","path":"/api/skills/import","queryParams":[]},
			properties: [
				{
					displayName: 'Github URL',
					name: 'githubUrl',
					type: 'string',
					required: true,
					description: 'Public GitHub repo URL, or a subfolder containing SKILL.md',
					default: '',
				}
			],
		},
		{
			value: 'listFileSkillFiles',
			name: 'List File Skill Files',
			action: 'The files of ONE file-skill, with their exact `path`, mime type, size and text content when the file is text',
			description: 'The files of ONE file-skill, with their exact `path`, mime type, size and text content when the file is text. It is the listing that `write_file_skill_file` and `delete_file_skill_file` both refer to: their `path` must match one of these EXACTLY, and until now no tool returned it — the deletion tool was blind by construction. Read it before rewriting or deleting a file, and to show the person what a skill actually contains before you change it. `ID` comes from `list_file_skills`. Owner only: a skill belonging to someone else answers 403, never a partial listing.',
			routeSpec: {"method":"GET","path":"/api/skills/{id}/files","queryParams":[]},
			properties: [
				{
					displayName: 'ID',
					name: 'id',
					type: 'string',
					required: true,
					description: 'Skills.ID (uuid), from list_file_skills',
					default: '',
				}
			],
		},
		{
			value: 'listFileSkills',
			name: 'List File Skills',
			action: 'The file-skills visible on one tab, with their `ID`, name, description, draft flag and enabled flag',
			description: 'The file-skills visible on one tab, with their `ID`, name, description, draft flag and enabled flag. THIS IS THE ONLY SOURCE OF A SKILL `ID`, and seven write tools need one — `update_file_skill`, `toggle_file_skill`, `delete_file_skill`, `write_file_skill_file`, `delete_file_skill_file`, `create_file_skill_folder` and, to read back what it just wrote, `create_file_skill`. Call it before any of them rather than inventing an ID. `tab` picks what you look at: `personal` (default, the person\'s own skills), `system` (the ones Aurentia ships), `team` (an agency or a project\'s shared skills — returns empty when no scope applies) or `marketplace` (the public catalogue). `projectId` narrows the `team` tab to one project. The response also carries the resolved `scope`, which says what `team` was computed against.',
			routeSpec: {"method":"GET","path":"/api/skills","queryParams":["tab","projectId"]},
			properties: [
				{
					displayName: 'Additional Fields',
					name: 'additionalFields',
					type: 'collection',
					placeholder: 'Add Field',
					default: {},
					options: [
						{
							displayName: 'Project ID',
							name: 'projectId',
							type: 'string',
							description: 'Optional — narrows the `team` tab to one project of the account',
							default: '',
						},
						{
							displayName: 'Tab',
							name: 'tab',
							type: 'options',
							description: 'Which shelf to read. Defaults to `personal`.',
							default: 'marketplace',
							options: [
								{ name: 'Marketplace', value: 'marketplace' },
								{ name: 'Personal', value: 'personal' },
								{ name: 'System', value: 'system' },
								{ name: 'Team', value: 'team' },
							],
						},
					],
				}
			],
		},
		{
			value: 'toggleFileSkill',
			name: 'Toggle File Skill',
			action: 'Enable or disable a file skill without deleting it: disabled, it is no longer injected into any collaborator\'s prompt and can no longer be loaded',
			description: 'Enable or disable a file skill without deleting it: disabled, it is no longer injected into any collaborator\'s prompt and can no longer be loaded. You send the state you want (not a toggle). Owner only. Different from `is_draft`, which is about publishing a draft (→ `update_file_skill`): a published skill that is disabled stays published.',
			routeSpec: {"method":"POST","path":"/api/skills/{id}/toggle","queryParams":[]},
			properties: [
				{
					displayName: 'ID',
					name: 'id',
					type: 'string',
					required: true,
					description: 'The ID for this operation',
					default: '',
				},
				{
					displayName: 'Enabled',
					name: 'enabled',
					type: 'boolean',
					required: true,
					description: 'Whether to enable enabled',
					default: false,
				}
			],
		},
		{
			value: 'updateFileSkill',
			name: 'Update File Skill',
			action: 'Change the METADATA of a file skill (SKILL.md): `name`; `description` — it is what triggers auto-loading, so write it as a « when to use this »; `trigger_mode` (`auto` = loaded by relevance, `command` = only by `/command`, `both`); `command_name` (lowercase-and-dashes); and above all `is_draft: false` to PUBLISH a draft created by the chat built-in `save_skill_draft` — without that step, a skill created in conversation stays inert for life: never injected, never loadable, never executed',
			description: 'Change the METADATA of a file skill (SKILL.md): `name`; `description` — it is what triggers auto-loading, so write it as a « when to use this »; `trigger_mode` (`auto` = loaded by relevance, `command` = only by `/command`, `both`); `command_name` (lowercase-and-dashes); and above all `is_draft: false` to PUBLISH a draft created by the chat built-in `save_skill_draft` — without that step, a skill created in conversation stays inert for life: never injected, never loadable, never executed. `is_draft: true` puts it back to draft. Owner only. To switch a skill off without deleting it, `toggle_file_skill`.',
			routeSpec: {"method":"PUT","path":"/api/skills/{id}","queryParams":[]},
			properties: [
				{
					displayName: 'ID',
					name: 'id',
					type: 'string',
					required: true,
					description: 'The ID for this operation',
					default: '',
				},
				{
					displayName: 'Additional Fields',
					name: 'additionalFields',
					type: 'collection',
					placeholder: 'Add Field',
					default: {},
					options: [
						{
							displayName: 'Command Name',
							name: 'command_name',
							type: 'string',
							default: '',
						},
						{
							displayName: 'Description',
							name: 'description',
							type: 'string',
							default: '',
						},
						{
							displayName: 'Is Draft',
							name: 'is_draft',
							type: 'boolean',
							description: 'Whether false = publish the draft. Without it a chat-created skill stays inert.',
							default: false,
						},
						{
							displayName: 'Name',
							name: 'name',
							type: 'string',
							default: '',
						},
						{
							displayName: 'Trigger Mode',
							name: 'trigger_mode',
							type: 'options',
							default: 'auto',
							options: [
								{ name: 'Auto', value: 'auto' },
								{ name: 'Both', value: 'both' },
								{ name: 'Command', value: 'command' },
							],
						},
					],
				}
			],
		},
		{
			value: 'writeFileSkillFile',
			name: 'Write File Skill File',
			action: 'Create or REPLACE a text file of a file skill (SKILL.md, a script, a reference)',
			description: 'Create or REPLACE a text file of a file skill (SKILL.md, a script, a reference). `content` is the COMPLETE content, not a diff — read the file back before rewriting it or you lose what you did not resend. Relative path, no `..`, 4 levels deep max, 5 MB per file. Changing SKILL.md changes the skill\'s behaviour from its next load, for every collaborator that carries it. For a binary (image, PDF) this is not the tool: that upload only exists in the app.',
			routeSpec: {"method":"PUT","path":"/api/skills/{id}/files","queryParams":[]},
			properties: [
				{
					displayName: 'ID',
					name: 'id',
					type: 'string',
					required: true,
					description: 'The ID for this operation',
					default: '',
				},
				{
					displayName: 'Path',
					name: 'path',
					type: 'string',
					required: true,
					description: 'Relative path inside the skill, e.g. SKILL.md or scripts/run.py',
					default: '',
				},
				{
					displayName: 'Content',
					name: 'content',
					type: 'string',
					required: true,
					description: 'Complete file content, never a diff',
					default: '',
				}
			],
		}
	],
};
